<UserControl x:Class="PharmacySalesApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PharmacySalesApp.Helper">

    <Grid Background="#EEF2F7">

        <Border Width="340"
                Padding="25"
                Background="White"
                CornerRadius="12"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <StackPanel>

                <TextBlock Text="ĐĂNG NHẬP"
                           FontSize="22"
                           FontWeight="Bold"
                           Margin="0,0,0,20"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Username" Margin="0,0,0,5"/>

                <TextBox Height="35"
                         Margin="0,0,0,15"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Password" Margin="0,0,0,5"/>

                <PasswordBox Height="35"
             Margin="0,0,0,20"
             local:PasswordHelper.BindPassword="True"
             local:PasswordHelper.BoundPassword="{Binding Password, Mode=TwoWay}" />


                <Button Content="LOGIN"
                        Height="38"
                        Background="#2D89EF"
                        Foreground="White"
                        FontWeight="Bold"
                        Command="{Binding LoginCommand}"/>

            </StackPanel>
        </Border>

    </Grid>
</UserControl>